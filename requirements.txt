# Python dependencies for TTG Distributed Worker
#
# Install with: pip install -r requirements.txt

# No external dependencies needed for basic worker
# The worker uses only Python standard library

# Uncomment below if you add features that need these:

# JSON Schema validation
# jsonschema>=4.17.0

# HTTP client (if workers need to call APIs)
# requests>=2.28.0

# Redis client (for Milestone 2 queue-based architecture)
redis>=4.5.0

# Message queue clients (for future iterations)
# celery>=5.2.0
# pika>=1.3.0  # RabbitMQ

# Azure integration (if using Azure services)
# azure-storage-blob>=12.0.0
# azure-servicebus>=7.8.0

# Observability (future milestone)
# prometheus-client>=0.16.0
# opentelemetry-api>=1.15.0
# opentelemetry-sdk>=1.15.0
